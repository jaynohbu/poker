AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: GameHub backend SAM infrastructure and deployment stack.

Parameters:
  EnvironmentName:
    Type: String
    Default: dev
  CorsOrigins:
    Type: String
    Default: http://localhost:55123,http://localhost:4200
  BlogImagesBucket:
    Type: String
    Default: gh_blog_images
  BlogImagesRegion:
    Type: String
    Default: ap-northeast-2
  BlogImagesPublicBaseUrl:
    Type: String
    Default: ''
  BlogTranslateRegion:
    Type: String
    Default: us-west-2

Globals:
  Function:
    Runtime: nodejs20.x
    Timeout: 30
    MemorySize: 512
    Architectures:
      - x86_64
    Environment:
      Variables:
        DYNAMODB_INIT: 'false'
        DYNAMODB_SEED: 'false'
        DYNAMODB_MIGRATE_ARTICLE_CONTENT: 'false'
        CORS_ORIGINS: !Ref CorsOrigins
        BLOG_IMAGES_BUCKET: !Ref BlogImagesBucket
        BLOG_IMAGES_REGION: !Ref BlogImagesRegion
        BLOG_IMAGES_PUBLIC_BASE_URL: !Ref BlogImagesPublicBaseUrl
        BLOG_TRANSLATE_REGION: !Ref BlogTranslateRegion

Resources:
  ApiGateway:
    Type: AWS::Serverless::HttpApi
    Properties:
      StageName: !Ref EnvironmentName

  ArticlesTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: !Sub gh_articles_${EnvironmentName}
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: slug
          AttributeType: S
      KeySchema:
        - AttributeName: slug
          KeyType: HASH

  BackendFunction:
    Type: AWS::Serverless::Function
    Metadata:
      BuildMethod: makefile
    Properties:
      CodeUri: .
      Handler: dist/lambda.handler
      Environment:
        Variables:
          ARTICLES_TABLE_NAME: !Ref ArticlesTable
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref ArticlesTable
        - Statement:
            - Effect: Allow
              Action:
                - translate:TranslateText
              Resource: '*'
      Events:
        Root:
          Type: HttpApi
          Properties:
            ApiId: !Ref ApiGateway
            Method: ANY
            Path: /
        Proxy:
          Type: HttpApi
          Properties:
            ApiId: !Ref ApiGateway
            Method: ANY
            Path: /{proxy+}

Outputs:
  ApiEndpoint:
    Description: Public URL of deployed backend API
    Value: !Sub https://${ApiGateway}.execute-api.${AWS::Region}.amazonaws.com/${EnvironmentName}
  ArticlesTableName:
    Description: DynamoDB table name for articles
    Value: !Ref ArticlesTable
